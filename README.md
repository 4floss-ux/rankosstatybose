# rankosstatybose.lt

MVP v0.2: React + Vite + Supabase Auth.

## Aplinkos kintamieji

Cloudflare build variables:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`

## Build

```bash
npm install
npm run build
```

Deploy:

```bash
npx wrangler deploy
```
